{%- comment -%}
  Critical CSS - Above the Fold Styles
  Extracted critical CSS pentru faster First Paint
  Generat automat sau manual din common.min.css, header.min.css, theme.min.css
{%- endcomment -%}

<style id="critical-css">
/* Reset & Base - Critical */
*,*::before,*::after{box-sizing:border-box}
body,html{margin:0;padding:0;min-height:100vh}
body{font-family:var(--font-body-family),sans-serif;font-size:var(--font-body-size);line-height:1.6;color:var(--color-text);background:var(--color-background)}

/* Layout Critical */
.wrapper{max-width:1400px;margin:0 auto;padding:0 20px}
header{position:relative;z-index:100}
.sticky-header header{position:sticky;top:0}

/* Header Critical Styles */
.yv-header{display:flex;align-items:center;justify-content:space-between;padding:15px 0}
.header-logo{max-width:200px}
.header-logo img{max-width:100%;height:auto}
.header-nav{display:flex;align-items:center;gap:20px}
.header-icons{display:flex;align-items:center;gap:15px}

/* Navigation Critical */
.nav-menu{display:flex;list-style:none;margin:0;padding:0;gap:25px}
.nav-menu a{text-decoration:none;color:inherit;font-weight:500}

/* Hero/Banner Critical */
.hero-section{position:relative;overflow:hidden}
.hero-image{width:100%;height:auto;display:block}

/* Product Grid Critical */
.product-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(250px,1fr));gap:20px}
.product-card{position:relative}
.product-image{width:100%;height:auto;display:block}

/* CTA Buttons Critical */
.btn,.button{display:inline-block;padding:12px 30px;background:var(--color-button);color:var(--color-button-text);text-decoration:none;border:none;cursor:pointer;transition:all .3s}
.btn:hover,.button:hover{opacity:.9;transform:translateY(-2px)}

/* Cart Icon Critical */
.cart-icon{position:relative}
.cart-count{position:absolute;top:-8px;right:-8px;background:#ff0000;color:#fff;border-radius:50%;width:20px;height:20px;display:flex;align-items:center;justify-content:center;font-size:11px}

/* Loading States */
.page-loading{overflow:hidden}
.yv-page-loader{position:fixed;width:100%;height:100%;background:rgba(255,255,255,.95);top:0;left:0;display:flex;align-items:center;justify-content:center;z-index:9999}

/* Mobile Menu Toggle */
.mobile-menu-toggle{display:none;background:none;border:none;padding:5px;cursor:pointer}
@media (max-width:768px){
  .mobile-menu-toggle{display:block}
  .nav-menu{display:none}
  .nav-open .nav-menu{display:flex;flex-direction:column;position:fixed;top:0;left:0;width:100%;height:100vh;background:#fff;padding:80px 20px;z-index:999}
}

/* Utility Classes Critical */
.hide{display:none!important}
.show{display:block!important}
.sr-only{position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0,0,0,0);border:0}

/* Font Loading Optimization */
@font-face{font-family:'system-ui';src:local('system-ui',-apple-system,'Segoe UI',Roboto,Helvetica,Arial,sans-serif);font-display:swap}

/* Prevent FOUT */
.fonts-loading body{opacity:0}
.fonts-loaded body{opacity:1;transition:opacity .2s}

/* Image Loading Placeholder */
img[data-src]{background:linear-gradient(90deg,#f0f0f0 25%,#e0e0e0 50%,#f0f0f0 75%);background-size:200% 100%;animation:loading 1.5s infinite}
@keyframes loading{0%{background-position:200% 0}100%{background-position:-200% 0}}

/* Performance - will-change hints */
.sticky-header header{will-change:transform}
.product-card:hover{will-change:transform}

/* Reduce Reflows */
.lazyload,.lazyloading{opacity:0}
.lazyloaded{opacity:1;transition:opacity .3s}
</style>

{%- comment -%}
  Note: Acest CSS critic ar trebui optimizat specific pentru site-ul tău
  Folosește tools precum:
  - https://jonassebastianohlsson.com/criticalpathcssgenerator/
  - https://www.sitelocity.com/critical-path-css-generator
  - Critical (npm package)
  
  Pentru a extrage CSS critic automat:
  1. Vizitează homepage-ul
  2. Folosește Chrome DevTools Coverage tab
  3. Identifică CSS folosit în above-the-fold
  4. Extrage și minifică
{%- endcomment -%}
